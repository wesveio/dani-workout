---
phase: 05-workout-templates
plan: 04
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified:
  - .planning/phases/05-workout-templates/05-04-SUMMARY.md
autonomous: false
requirements: [TMPL-01, TMPL-02, TMPL-03, TMPL-04]

must_haves:
  truths:
    - "All template flows work end-to-end: create, save-from-workout, start-from-template, edit, duplicate, delete"
    - "Full test suite passes with no regressions"
    - "Templates tab, preview sheet, and builder sheet render correctly on mobile viewport"
  artifacts:
    - path: ".planning/phases/05-workout-templates/05-04-SUMMARY.md"
      provides: "Phase 5 verification report confirming all template flows"
  key_links: []
---

<objective>
Run full verification suite and human visual/functional checkpoint for all template features.

Purpose: Confirms all four TMPL requirements work end-to-end before phase completion.

Output: Verification report confirming all flows work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-workout-templates/05-CONTEXT.md
@.planning/phases/05-workout-templates/05-01-SUMMARY.md
@.planning/phases/05-workout-templates/05-02-SUMMARY.md
@.planning/phases/05-workout-templates/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated verification</name>
  <files>.planning/phases/05-workout-templates/05-04-SUMMARY.md</files>
  <read_first>
    src/data/exerciseCatalog.test.ts
    src/store/workoutStore.test.ts
    src/store/workoutStore.template.test.ts
  </read_first>
  <action>
Run the complete test suite and verify no regressions:

1. `npx vitest run --reporter=verbose` -- all tests must pass
2. `npx tsc --noEmit` -- no TypeScript errors
3. `npx vite build` -- production build succeeds with no errors

If any test fails, investigate and fix before proceeding to human verification.

Verify key integration points:
- exerciseCatalog exports are importable and non-empty
- workoutStore template actions exist in the store type
- Templates route is lazy-loaded in App.tsx
- SessionDetail handles 'template' sessionId without crashing
  </action>
  <verify>
    <automated>cd /Users/wesley/Documents/Personal/projects/workout/dani && npx vitest run --reporter=verbose && npx tsc --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>Full test suite, type check, and production build all pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete template feature: exercise catalog, template CRUD, save-from-workout, start-from-template with preview/edit, Templates tab with card list and builder</what-built>
  <how-to-verify>
    1. Open app in browser at localhost (run `npx vite --open` if not running)
    2. Navigate to Templates tab -- verify empty state shows "Nenhum template ainda" with "Criar Template" button
    3. Tap "Criar Template" -- builder sheet opens with name field and "Adicionar Exercicio" button
    4. Add 3-4 exercises from different muscle groups, configure set counts, enter a name, tap "Criar Template"
    5. Verify template card appears in list with correct name, exercise count, and muscle group badges
    6. Tap overflow menu (3 dots) on template card -- verify Edit, Duplicar, Excluir options appear
    7. Tap "Duplicar" -- verify duplicate card appears with "(copia)" suffix
    8. Tap "Excluir" on duplicate -- verify card removes
    9. Tap "Iniciar" on remaining template -- preview sheet opens showing exercises and set counts
    10. Tap "Iniciar Treino" -- session starts with exercises from template, inputs pre-filled
    11. Complete the session (fill a few sets, tap finish) -- verify "Salvar como Template" button appears in celebration overlay
    12. Tap "Salvar como Template" -- dialog shows with auto-generated name, save it
    13. Navigate to Templates tab -- verify new template from workout appears in list
    14. Go to home screen (Dashboard) -- verify template buttons appear under "Novo Treino" area
    15. Tap a template from Dashboard -- preview sheet opens, tap "Iniciar Treino" to start

    Check on mobile viewport (Chrome DevTools device toolbar, iPhone 14 Pro):
    - Cards are readable and touch targets are at least 44px
    - Builder sheet scrolls properly on small screen
    - Exercise picker muscle group tabs scroll horizontally
    - Swipe left on template card reveals delete zone (if implemented)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in verification plan.

## STRIDE Threat Register

No new threats -- verification only.
</threat_model>

<verification>
- Full test suite passes
- TypeScript compiles
- Production build succeeds
- Human verifies all template flows work on mobile viewport
</verification>

<success_criteria>
- Zero test failures
- Zero TypeScript errors
- Zero build errors
- Human approves all 15 verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/05-workout-templates/05-04-SUMMARY.md`
</output>
