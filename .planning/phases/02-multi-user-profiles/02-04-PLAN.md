---
phase: 02-multi-user-profiles
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/data/users.ts
autonomous: false
requirements: [PROF-01, PROF-02, PROF-03, PROF-04]
must_haves:
  truths:
    - "data/users.ts no longer exports userList, defaultUserId, getUserProfile"
    - "No file in src/ imports from @/data/users except useActiveProgram's program lookup"
    - "All profile flows work end-to-end: create, switch, edit, delete"
  artifacts:
    - path: "src/data/users.ts"
      provides: "Cleaned up file — only program-related exports remain (if any)"
  key_links:
    - from: "src/data/users.ts"
      to: "nowhere"
      via: "No remaining profile-related consumers"
      pattern: "userList|defaultUserId|getUserProfile"
---

<objective>
Clean up data/users.ts by removing dead profile-related exports, then verify the entire profile system end-to-end with a human checkpoint.

Purpose: Removes legacy code that could confuse future phases and validates all profile flows visually.
Output: Clean data/users.ts, verified working profile system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-user-profiles/02-CONTEXT.md
@.planning/phases/02-multi-user-profiles/02-01-SUMMARY.md
@.planning/phases/02-multi-user-profiles/02-02-SUMMARY.md
@.planning/phases/02-multi-user-profiles/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up data/users.ts dead exports</name>
  <files>src/data/users.ts</files>
  <read_first>
    - src/data/users.ts (current file with all exports)
    - src/lib/user.ts (verify it no longer imports from data/users — should import from treinoDani/treinoWesley directly after Plan 01)
    - src/store/workoutStore.ts (verify defaultUserId import removed after Plan 01)
    - src/components/Layout.tsx (verify userList import removed after Plan 02)
  </read_first>
  <action>
**1. Verify no remaining consumers of profile exports:**
Run grep across src/ for imports from `@/data/users`:
- `grep -r "from '@/data/users'" src/` or `grep -r 'from "@/data/users"' src/`
- Expected: NO results (all consumers migrated in Plans 01-03)
- If any consumer remains, migrate it before proceeding

**2. If no consumers import from data/users.ts at all:**
Delete the file entirely: `rm src/data/users.ts`

The file exported: `UserProfile` type, `users` record, `userList`, `defaultUserId`, `getUserProfile`, `getProgramForUser`. After Plans 01-03:
- `defaultUserId` removed from workoutStore.ts (Plan 01)
- `userList` removed from Layout.tsx (Plan 02)
- `getUserProfile` removed from lib/user.ts (Plan 01)
- `useActiveProgram` now imports directly from `@/data/treinoDani` and `@/data/treinoWesley` (Plan 01)

**3. If any consumer still imports from data/users.ts (unexpected):**
Migrate that consumer to use the appropriate direct import, THEN delete the file.

**4. Verify no TypeScript errors:**
Run `npx tsc --noEmit` to confirm clean compilation after deletion.

**5. Run full test suite:**
Run `npm run test:run` to confirm no regressions.
  </action>
  <verify>
    <automated>! test -f src/data/users.ts && npx tsc --noEmit 2>&1 | head -10 && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - src/data/users.ts does NOT exist (deleted)
    - No file in src/ contains `from '@/data/users'` or `from "@/data/users"`
    - `npx tsc --noEmit` exits 0 (no type errors)
    - `npm run test:run` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>data/users.ts deleted. No remaining profile-related imports from the old hardcoded module. TypeScript and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete multi-user profile system: profile switcher dropdown in header (replaces old select), create-profile dialog, profile editing in Settings (name + avatar color), delete-profile with type-to-confirm, active-workout switch guard, data isolation per profile.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. **Profile switcher:** Tap the avatar circle in the header — verify dropdown shows dani and wesley with color dots, checkmark on active
    3. **Switch profile:** Tap the other profile — verify header avatar changes, workouts reload for that user
    4. **Create profile:** Tap "+ Novo perfil" in dropdown — verify dialog opens with name input and avatar preview. Type a name (e.g. "Carlos"), verify preview updates live. Click "Criar perfil" — verify dropdown now shows 3 profiles, new profile is active
    5. **New profile safety:** Verify the app does NOT crash with the new profile active (no program = graceful handling in header)
    6. **Edit profile:** Go to Settings (Config tab) — verify "Meu Perfil" section appears at top with name field pre-filled, 6 color dots with current color selected. Change the name and color, click "Salvar perfil" — verify avatar in header updates
    7. **Delete guard:** With only 1 profile remaining, verify "Excluir perfil" button is disabled
    8. **Delete flow:** Create a second test profile. Go to Settings, click "Excluir perfil" — verify dialog asks to type profile name. Type wrong name — verify confirm button stays disabled. Type correct name — click delete — verify profile removed and switched to remaining profile
    9. **Active workout guard:** Start a workout session (navigate to /session), then try switching profiles via dropdown — verify it is blocked with warning message "Finalize ou descarte o treino antes de trocar de perfil"
    10. **Persistence:** Refresh the page — verify last active profile is remembered
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Cleanup task + visual verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | Deleted users.ts | accept | File contained no secrets — only hardcoded profile metadata. Removal reduces attack surface. |
</threat_model>

<verification>
```bash
npx tsc --noEmit && npm run test:run
```
Plus human verification of all profile flows.
</verification>

<success_criteria>
- data/users.ts deleted with no remaining consumers
- All profile CRUD flows verified visually by human
- Active workout guard blocks profile switching
- Profile persistence across page refresh confirmed
- App does not crash for profiles without linked programs
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-user-profiles/02-04-SUMMARY.md`
</output>
