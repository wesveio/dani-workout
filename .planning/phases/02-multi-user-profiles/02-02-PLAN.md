---
phase: 02-multi-user-profiles
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/ProfileSwitcher.tsx
  - src/components/CreateProfileDialog.tsx
  - src/components/Layout.tsx
autonomous: true
requirements: [PROF-01, PROF-02]
must_haves:
  truths:
    - "User sees avatar circle trigger in header that opens a dropdown with all profiles"
    - "Each profile in dropdown shows color dot + name + checkmark on active profile"
    - "User can tap a different profile to switch (calls switchUser)"
    - "User sees '+ Novo perfil' at bottom of dropdown separated by divider"
    - "Tapping '+ Novo perfil' opens a Dialog to create a new profile with name input"
    - "After creating a profile, dropdown updates reactively with the new entry"
    - "Layout.tsx gracefully handles program === null for new profiles"
  artifacts:
    - path: "src/components/ProfileSwitcher.tsx"
      provides: "DropdownMenu with profile list, avatar trigger, create profile entry point"
    - path: "src/components/CreateProfileDialog.tsx"
      provides: "Dialog for new profile creation with name input and avatar preview"
    - path: "src/components/Layout.tsx"
      provides: "Updated header using ProfileSwitcher instead of select, null-guarded program references"
  key_links:
    - from: "src/components/ProfileSwitcher.tsx"
      to: "src/store/workoutStore.ts"
      via: "useWorkoutStore selectors for switchUser, activeUserId, createProfile"
      pattern: "useWorkoutStore"
    - from: "src/components/ProfileSwitcher.tsx"
      to: "src/db/client.ts"
      via: "useLiveQuery(() => db.profiles.toArray())"
      pattern: "useLiveQuery.*db\\.profiles"
    - from: "src/components/Layout.tsx"
      to: "src/components/ProfileSwitcher.tsx"
      via: "import ProfileSwitcher"
      pattern: "ProfileSwitcher"
---

<objective>
Build the ProfileSwitcher dropdown component and CreateProfileDialog, then replace the hardcoded `<select>` in Layout.tsx with the new ProfileSwitcher. Add null guards for `program` references in Layout.tsx so new profiles (without programs) do not crash the app.

Purpose: Delivers the visible profile switching and creation UX (PROF-01, PROF-02).
Output: Working profile switcher dropdown in header, create-profile modal, crash-safe Layout for programless profiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-user-profiles/02-CONTEXT.md
@.planning/phases/02-multi-user-profiles/02-PATTERNS.md
@.planning/phases/02-multi-user-profiles/02-UI-SPEC.md
@.planning/phases/02-multi-user-profiles/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor should read these files first -->

From src/store/workoutStore.ts (after Plan 01):
```typescript
type WorkoutStore = {
  // ... existing fields ...
  activeUserId: UserId
  loading: boolean
  error: string | null
  switchUser: (userId: UserId) => Promise<void>
  createProfile: (name: string) => Promise<void>
  // switchUser now blocks during /session routes
}
```

From src/lib/user.ts (after Plan 01):
```typescript
export const useActiveUserProfile = (): Profile | undefined => { /* Dexie live query */ }
export const useActiveProgram = (): Program | null => { /* null for non-dani/wesley */ }
```

From src/lib/profile-constants.ts (created in Plan 01):
```typescript
export const AVATAR_COLORS: readonly string[]
export const pickColor: (existingCount: number) => string
```

From src/components/ui/dropdown-menu.tsx (installed in Plan 01):
```typescript
export { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuTrigger }
```

From src/types.ts:
```typescript
export type Profile = { id: string; name: string; shortName: string; avatarInitial: string; avatarColor: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileSwitcher and CreateProfileDialog components</name>
  <files>src/components/ProfileSwitcher.tsx, src/components/CreateProfileDialog.tsx</files>
  <read_first>
    - src/components/Layout.tsx (current avatar + select block to replace, lines 55-79)
    - src/components/ui/dropdown-menu.tsx (verify exports after Plan 01 install)
    - src/routes/Settings.tsx (Dialog pattern reference, lines 151-176)
    - src/store/workoutStore.ts (verify createProfile and switchUser exist after Plan 01)
    - src/lib/user.ts (verify useActiveUserProfile returns Profile | undefined)
    - .planning/phases/02-multi-user-profiles/02-UI-SPEC.md (interaction states, copywriting, colors)
    - .planning/phases/02-multi-user-profiles/02-PATTERNS.md (ProfileSwitcher and CreateProfileDialog patterns)
  </read_first>
  <action>
**Create `src/components/CreateProfileDialog.tsx`:**

Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`

Implementation:
- `useState` for `name` (string, default '')
- Get `createProfile` from `useWorkoutStore((s) => s.createProfile)`
- Get `profiles` from `useLiveQuery(() => db.profiles.toArray(), []) ?? []` (needed for avatar preview color)
- Import `pickColor` from `@/lib/profile-constants`
- Compute live preview: `avatarInitial = name.trim().charAt(0).toUpperCase() || '?'`, `avatarColor = pickColor(profiles.length)`
- `handleCreate`: trim name, reject if empty or length > 50, call `await createProfile(trimmed)`, reset name to '', call `onOpenChange(false)`, show toast `{ title: 'Perfil criado', description: '${trimmed} adicionado.' }`
- JSX structure (from UI-SPEC):
  - `<Dialog open={open} onOpenChange={onOpenChange}>`
  - `<DialogContent>` with `<DialogHeader>`: title "Novo perfil", description "Escolha um nome para comecar."
  - Avatar preview: centered 44px color circle (`h-11 w-11 rounded-2xl grid place-items-center font-bold`) with `style={{ backgroundColor: avatarColor, color: '#161616' }}` showing `avatarInitial`
  - `<Input value={name} onChange={...} placeholder="Nome do perfil" maxLength={50} />`
  - Button row: "Nao criar" (variant="secondary", onClick close) + "Criar perfil" (disabled when `name.trim().length === 0`, accent styling)
- Import `toast` from `@/components/ui/use-toast`
- Import Dialog parts from `@/components/ui/dialog`
- Import `useLiveQuery` from `dexie-react-hooks`, `db` from `@/db/client`

**Create `src/components/ProfileSwitcher.tsx`:**

Implementation:
- `useState` for `createOpen` (boolean, default false)
- Get `profiles` from `useLiveQuery(() => db.profiles.toArray(), []) ?? []`
- Get `activeUserId` from `useWorkoutStore((s) => s.activeUserId)`
- Get `switchUser` from `useWorkoutStore((s) => s.switchUser)`
- Get `error` from `useWorkoutStore((s) => s.error)` (for displaying switchUser block message)
- Find `activeProfile = profiles.find((p) => p.id === activeUserId)`
- Detect active session: `const isSessionActive = typeof window !== 'undefined' && window.location.pathname.startsWith('/session')`
- Import `Check` from `lucide-react`
- JSX structure (from UI-SPEC and PATTERNS.md):
  - `<DropdownMenu>` wrapping trigger + content + CreateProfileDialog
  - Trigger: `<DropdownMenuTrigger asChild><button className="h-11 w-11 rounded-2xl grid place-items-center font-bold" style={{ backgroundColor: activeProfile?.avatarColor ?? '#666', color: '#161616' }} aria-label="Trocar perfil">{activeProfile?.avatarInitial ?? '?'}</button></DropdownMenuTrigger>`
  - Content: `<DropdownMenuContent align="start">` with profile list items
  - Each profile item: `<DropdownMenuItem key={p.id} onClick={() => switchUser(p.id)} disabled={isSessionActive && p.id !== activeUserId}>` containing:
    - Color dot: `<span className="h-3 w-3 rounded-full mr-2 shrink-0" style={{ backgroundColor: p.avatarColor }} />`
    - Name: `{p.shortName}`
    - Checkmark on active: `{p.id === activeUserId && <Check className="ml-auto h-4 w-4 text-[#4EFF74]" />}`
  - If `isSessionActive && error`: show warning text below items: `<p className="px-2 py-1 text-xs text-[#FF4444]">Finalize ou descarte o treino antes de trocar de perfil</p>`
  - Separator: `<DropdownMenuSeparator />`
  - Add profile: `<DropdownMenuItem onClick={() => setCreateOpen(true)} className="text-[#4EFF74]">+ Novo perfil</DropdownMenuItem>`
  - After DropdownMenu: `<CreateProfileDialog open={createOpen} onOpenChange={setCreateOpen} />`
  </action>
  <verify>
    <automated>grep -q "DropdownMenu" src/components/ProfileSwitcher.tsx && grep -q "CreateProfileDialog" src/components/ProfileSwitcher.tsx && grep -q "Dialog" src/components/CreateProfileDialog.tsx && grep -q "createProfile" src/components/CreateProfileDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ProfileSwitcher.tsx exports `ProfileSwitcher` function component
    - src/components/ProfileSwitcher.tsx contains `useLiveQuery(() => db.profiles.toArray()`
    - src/components/ProfileSwitcher.tsx contains `DropdownMenuTrigger` with `aria-label="Trocar perfil"`
    - src/components/ProfileSwitcher.tsx contains `+ Novo perfil` text
    - src/components/ProfileSwitcher.tsx contains `Check` import from lucide-react
    - src/components/ProfileSwitcher.tsx contains `text-[#4EFF74]` for accent color on checkmark and add-profile item
    - src/components/ProfileSwitcher.tsx contains disabled logic for session-active state
    - src/components/CreateProfileDialog.tsx exports `CreateProfileDialog` function component
    - src/components/CreateProfileDialog.tsx contains `pickColor` import from `@/lib/profile-constants`
    - src/components/CreateProfileDialog.tsx contains input with `placeholder="Nome do perfil"` and `maxLength={50}`
    - src/components/CreateProfileDialog.tsx contains "Criar perfil" button text
    - src/components/CreateProfileDialog.tsx contains "Nao criar" or "Cancelar" secondary button
    - src/components/CreateProfileDialog.tsx contains toast call with title 'Perfil criado'
  </acceptance_criteria>
  <done>ProfileSwitcher and CreateProfileDialog components created with full UI-SPEC compliance, wired to store actions and Dexie live queries.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Layout.tsx select with ProfileSwitcher + null-guard program</name>
  <files>src/components/Layout.tsx</files>
  <read_first>
    - src/components/Layout.tsx (full file — understand all program references)
    - src/components/ProfileSwitcher.tsx (created in Task 1)
    - src/lib/user.ts (useActiveProgram now returns Program | null)
    - .planning/phases/02-multi-user-profiles/02-PATTERNS.md (Layout.tsx modification section)
  </read_first>
  <action>
**Modify `src/components/Layout.tsx`:**

1. **Remove imports:**
   - Remove: `import { userList } from '@/data/users'`

2. **Add imports:**
   - Add: `import { ProfileSwitcher } from './ProfileSwitcher'`

3. **Null-guard all program references (program is now `Program | null`):**
   - Line 32-33: Change `const weekNumber = getCurrentWeekNumber(settings.programStart, program.durationWeeks)` to `const weekNumber = program ? getCurrentWeekNumber(settings.programStart, program.durationWeeks) : 0`
   - Line 36: Change `const weekInfo = program.weeks.find(...)` to `const weekInfo = program?.weeks.find((w) => w.number === weekNumber)`
   - Line 38: Change `const scheduleLabel = program.schedule.map(...)` to `const scheduleLabel = program?.schedule.map((day) => day.day.slice(0, 3)).join(' / ') ?? ''`

4. **Null-guard profile references (profile is now `Profile | undefined`):**
   - Line 57: `profile.avatarInitial` -> `profile?.avatarInitial ?? '?'` (but this is now inside ProfileSwitcher, so remove avatar div)
   - Remove the entire avatar div (line 56-58) and the select block (lines 60-78)

5. **Replace the avatar div + select + "Perfil ativo" span (lines 55-79) with:**
   ```tsx
   <ProfileSwitcher />
   ```

6. **Guard program-dependent renders in the non-session header block (lines 92-103):**
   - Wrap the program name display: `{program && <div className="text-lg font-semibold leading-tight text-foreground">{program.name}</div>}`
   - Guard scheduleLabel: only render if scheduleLabel is non-empty
   - The profile.name display on line 95 becomes `profile?.name ?? ''`

7. **Guard weekInfo-dependent renders:**
   - The session header (lines 85-89): `{weekInfo && (...)}`  — already conditional, OK
   - Badge on line 107: already wrapped in `!isSession && weekInfo &&` — OK

8. **Remove `switchUser` selector** (line 30) — no longer needed directly in Layout since ProfileSwitcher handles it

9. **Remove `loading` selector** (line 31) if only used by the old select's `disabled` prop

After changes, Layout.tsx should NOT import from `@/data/users` at all.
  </action>
  <verify>
    <automated>grep -q "ProfileSwitcher" src/components/Layout.tsx && ! grep -q "userList" src/components/Layout.tsx && ! grep -q "<select" src/components/Layout.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Layout.tsx contains `import { ProfileSwitcher } from './ProfileSwitcher'`
    - src/components/Layout.tsx does NOT contain `import { userList } from '@/data/users'`
    - src/components/Layout.tsx does NOT contain `<select` element
    - src/components/Layout.tsx does NOT contain `<option` element
    - src/components/Layout.tsx contains `program ?` or `program?.` null guards (at least 3 occurrences)
    - src/components/Layout.tsx contains `<ProfileSwitcher />` in JSX
    - `npx tsc --noEmit` produces no type errors related to Layout.tsx
  </acceptance_criteria>
  <done>Layout.tsx uses ProfileSwitcher instead of hardcoded select. All program references null-guarded. No imports from data/users.ts remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> createProfile | Name from Dialog text input crosses into store action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | ProfileSwitcher switchUser | accept | Local-only app, no remote identity — switching profiles is the intended behavior |
| T-02-06 | Tampering | CreateProfileDialog name input | mitigate | maxLength={50} on input element + trim + empty check in createProfile action (enforced in Plan 01 store layer) |
</threat_model>

<verification>
```bash
npx tsc --noEmit && npm run test:run
```
TypeScript compiles without errors. All existing + new tests pass.
</verification>

<success_criteria>
- ProfileSwitcher renders in header with avatar trigger
- Dropdown shows all profiles from Dexie with color dots and checkmark on active
- "+ Novo perfil" item opens CreateProfileDialog
- CreateProfileDialog creates profile via store action
- Layout.tsx has no program/profile null crashes
- No imports from data/users.ts in Layout.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-user-profiles/02-02-SUMMARY.md`
</output>
