---
phase: 2
slug: multi-user-profiles
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-21
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Multi-User Profiles.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | manual (shadcn components without components.json) | codebase |
| Preset | not applicable | codebase |
| Component library | shadcn/ui (Dialog, DropdownMenu, Button, Input, Label) | CONTEXT.md D-01, D-04 |
| Icon library | lucide-react | codebase (Layout.tsx) |
| Font | Space Grotesk 400/500/600/700 | tailwind.config.js |
| Theme | Always dark — no light mode | REQUIREMENTS.md UI-01 |

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, color dot to label gaps |
| sm | 8px | Avatar circle padding, input inline padding |
| md | 16px | Dialog content padding, dropdown item height |
| lg | 24px | Dialog section vertical gap |
| xl | 32px | Modal max-width internal padding |
| 2xl | 48px | Vertical separation between dialog sections |
| 3xl | 64px | Not used this phase |

Exceptions:
- Touch targets minimum 44px height for all interactive dropdown items (mobile gym use, REQUIREMENTS.md UI-03)
- Avatar circle: 44px × 44px in header (matches existing h-11 w-11 = 44px)

---

## Typography

All sizes use Space Grotesk. Two weights only across all elements.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 | 1.5 | Dialog body text, dropdown profile names |
| Label | 14px | 400 | 1.4 | Helper text, "Perfil ativo" caption, input labels |
| Heading | 20px | 600 | 1.2 | Dialog title, Settings section heading |
| Display | 28px | 600 | 1.2 | Not used this phase |

Source: Established by codebase (index.css, Layout.tsx text-xs/text-sm/text-lg patterns).

---

## Color

All values from tailwind.config.js. Always-dark — no light surface used.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #161616 (background) | App background, dialog overlay base |
| Secondary (30%) | #1F1F1F (surface/card) | Dropdown panel, dialog content area, input backgrounds |
| Accent (10%) | #4EFF74 (accent) | Active profile checkmark, confirm button on create profile CTA, active nav item |
| Destructive | #FF4444 (implicit red) | Delete profile button, type-to-confirm input border when mismatch |
| Border/Divider | #2C2C2C (neutral) | Dropdown dividers, dialog border, input borders |
| Muted text | #CECECE (muted) | Disabled states, placeholder text, caption text |

Accent reserved for:
1. Checkmark indicator on currently active profile in dropdown list
2. Primary CTA button ("Criar perfil") in create-profile dialog
3. Do NOT use accent on avatar color dots — avatar colors use the preset palette (see below)

Avatar color palette (Claude's Discretion from CONTEXT.md — 6 colors for color circle system, dark-theme-safe):

| Name | Hex | Initial text color |
|------|-----|-------------------|
| Emerald | #2DD4BF | #161616 (dark) |
| Violet | #818CF8 | #161616 (dark) |
| Rose | #FB7185 | #161616 (dark) |
| Amber | #FCD34D | #161616 (dark) |
| Sky | #38BDF8 | #161616 (dark) |
| Orange | #FB923C | #161616 (dark) |

Auto-assignment algorithm: round-robin by existing profile count (0 → Emerald, 1 → Violet, …, 6+ wraps). Deterministic, not random — prevents repeated colors on a small list.

---

## Component Inventory

Components required this phase (all already installed in src/components/ui/):

| Component | Usage | Already Exists |
|-----------|-------|---------------|
| DropdownMenu | Profile switcher in header (replaces `<select>`) | No — add via shadcn |
| Dialog | Create-profile modal | Yes (dialog.tsx) |
| Input | Profile name field in create dialog, name edit in Settings | Yes (input.tsx) |
| Button | "Criar perfil" CTA, "Não criar" secondary, delete action | Yes (button.tsx) |
| Label | Form field label in dialog | Yes (label.tsx) |

DropdownMenu is not yet in src/components/ui/ — must be added. No third-party registry needed; use shadcn official.

---

## Interaction States

### Profile Switcher Dropdown (replaces `<select>` in header)

- **Trigger:** Tap avatar circle (44×44px) in header
- **Open state:** Panel opens below avatar, max-height 280px, overflow-y scroll if >4 profiles
- **Profile list item:** 44px min-height, avatar color dot (24px circle) + profile name (16px/400) + checkmark (accent #4EFF74, `aria-label="Perfil ativo"`) on active profile
- **"+ Novo perfil" item:** Bottom of list, separated by 1px divider (#2C2C2C), text-accent (#4EFF74), 44px min-height
- **Blocked state (active workout):** All non-active profiles show disabled style (text-muted, cursor-not-allowed). Tap shows inline warning text below dropdown: "Finalize ou descarte o treino antes de trocar de perfil" (text-[#FF4444], 14px)
- **Animation:** Dropdown slides down 4px + fades in, 150ms ease-out

### Create Profile Dialog

- **Trigger:** Tap "+ Novo perfil" in dropdown
- **State 1 — Empty:** Input placeholder "Nome do perfil", CTA button disabled (opacity-40)
- **State 2 — Filled:** Input has value, CTA button enabled (accent background, dark text)
- **State 3 — Saving:** CTA button shows spinner (lucide `Loader2` icon, animate-spin), disabled
- **State 4 — Success:** Dialog closes, new profile is automatically active, dropdown trigger updates to new avatar initial + color
- **Avatar preview:** 44px color circle with initial letter shown in dialog, updates live as user types name. Color auto-assigned from palette (not user-selectable at creation — editable later in Settings per D-08)
- **Primary focal point:** Avatar color preview circle — largest visual element in dialog, centered above the name input field. Eye lands here first, then moves down to the input.

### Profile Edit (Settings — "Meu Perfil" section)

- **Name field:** Text input, current name pre-filled, 16px/400
- **Avatar color:** Row of 6 color dots (32px circles), tap to select, selected has 2px white ring + checkmark overlay
- **Save:** "Salvar perfil" button, accent variant, enabled when any change detected
- **Layout:** Stacked vertically with lg (24px) gap between name field and color picker row

### Delete Profile

- **Entry point:** "Excluir perfil" button in Settings, destructive variant (text-[#FF4444], border-[#FF4444])
- **Disabled state:** Button disabled + tooltip "Você precisa de pelo menos um perfil." when only 1 profile exists
- **Confirmation dialog:** Title "Excluir [Nome]?", body "Esta ação não pode ser desfeita. Todos os dados de treino deste perfil serão apagados permanentemente."
- **Type-to-confirm field:** Input with placeholder "Digite [Nome] para confirmar", label "Nome do perfil"
- **Confirm button:** "Excluir perfil" — disabled (opacity-40) until input matches profile name exactly (case-sensitive). When enabled: background #FF4444, text #161616
- **After deletion:** Dialog closes, active profile auto-switches to first remaining profile, header updates immediately

---

## Copywriting Contract

All copy in Brazilian Portuguese (consistent with existing app language observed in Layout.tsx).

| Element | Copy |
|---------|------|
| Primary CTA (create profile) | "Criar perfil" |
| Create dialog title | "Novo perfil" |
| Create dialog input label | "Nome" |
| Create dialog input placeholder | "Nome do perfil" |
| Create dialog cancel | "Não criar" |
| Dropdown item — add profile | "+ Novo perfil" |
| Dropdown active label | (checkmark icon, `aria-label="Perfil ativo"`) |
| Active workout block warning | "Finalize ou descarte o treino antes de trocar de perfil" |
| Settings section heading | "Meu Perfil" |
| Settings save CTA | "Salvar perfil" |
| Settings avatar color label | "Cor do avatar" |
| Delete entry button | "Excluir perfil" |
| Delete dialog title | "Excluir [Nome]?" |
| Delete dialog body | "Esta ação não pode ser desfeita. Todos os dados de treino deste perfil serão apagados permanentemente." |
| Delete type-to-confirm label | "Digite [Nome] para confirmar" |
| Delete type-to-confirm placeholder | "[Nome]" |
| Delete confirm button | "Excluir perfil" |
| Delete blocked tooltip | "Você precisa de pelo menos um perfil." |
| Empty state (no profiles — impossible state) | Not applicable — app always has at least 1 profile |
| Error: name already taken | "Já existe um perfil com esse nome." |
| Error: name too short | "O nome precisa ter pelo menos 1 caractere." |
| Error: save failed | "Não foi possível salvar. Tente novamente." |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | DropdownMenu | not required — official registry |

No third-party registries declared for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
