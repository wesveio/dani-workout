---
phase: 03-ui-redesign-rest-timer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.js
  - src/index.css
  - src/components/Layout.tsx
  - src/App.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "App displays bold always-dark theme with red/orange accent palette"
    - "Navigation has 5 tabs: Inicio, Treino, Historico, Templates (placeholder), Config"
    - "All nav tabs have 44px+ touch targets"
    - "Header shows app name 'Dani' and ProfileSwitcher"
    - "Templates tab route redirects to home (placeholder for Phase 5)"
  artifacts:
    - path: "tailwind.config.js"
      provides: "New color token values"
      contains: "'#0D0D0D'"
    - path: "src/index.css"
      provides: "Updated body gradient, timer animations"
      contains: "rgba(255,61,61"
    - path: "src/components/Layout.tsx"
      provides: "5-tab nav with accent dot, simplified header"
      contains: "BarChart2"
    - path: "src/App.tsx"
      provides: "Templates placeholder route"
      contains: "/templates"
  key_links:
    - from: "tailwind.config.js"
      to: "all components"
      via: "Tailwind token compilation"
      pattern: "bg-accent|text-foreground|bg-surface"
    - from: "src/components/Layout.tsx"
      to: "src/App.tsx"
      via: "Route rendering inside Layout children"
      pattern: "navItems.*LayoutTemplate"
---

<objective>
Overhaul the visual theme to a bold dark palette with red/orange accents, restructure navigation to a future-proof 5-tab layout, and optimize all touch targets for mobile gym use.

Purpose: Delivers UI-01 (bold dark theme), UI-02 (restructured navigation), UI-03 (mobile-first optimization) -- the visual foundation that all subsequent Phase 3 work inherits.
Output: Updated tailwind.config.js, index.css, Layout.tsx (header + nav), App.tsx (templates placeholder route).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-redesign-rest-timer/03-CONTEXT.md
@.planning/phases/03-ui-redesign-rest-timer/03-RESEARCH.md
@.planning/phases/03-ui-redesign-rest-timer/03-PATTERNS.md
@.planning/phases/03-ui-redesign-rest-timer/03-UI-SPEC.md

<interfaces>
<!-- Current tailwind tokens (replace values, keep keys) -->
From tailwind.config.js:
```js
colors: {
  background: '#161616',
  foreground: '#F7F7F5',
  surface: '#1F1F1F',
  muted: '#CECECE',
  accent: '#4EFF74',
  accentSecondary: '#4495FF',
  neutral: '#2C2C2C',
  card: '#1F1F1F',
}
```

From src/components/Layout.tsx:
```tsx
const navItems = [
  { to: '/', label: 'Inicio', icon: Home },
  { to: '/week', label: 'Semana', icon: CalendarDays },
  { to: '/progress', label: 'Historico', icon: Dumbbell },
  { to: '/settings', label: 'Config', icon: SettingsIcon },
];
```

From src/App.tsx routes:
```tsx
<Route path="/" element={<Dashboard />} />
<Route path="/week" element={<WeekView />} />
<Route path="/session/:sessionId/:weekNumber?" element={<SessionDetail />} />
<Route path="/exercise/:exerciseId" element={<ExerciseHistory />} />
<Route path="/progress" element={<Progress />} />
<Route path="/settings" element={<Settings />} />
<Route path="*" element={<Navigate to="/" replace />} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace color tokens and update CSS globals</name>
  <files>tailwind.config.js, src/index.css</files>
  <read_first>tailwind.config.js, src/index.css</read_first>
  <action>
**tailwind.config.js** -- Replace ONLY the color hex values inside `colors: {}`. Do NOT rename any keys. New values per UI-SPEC Color section (D-01, D-02, D-04):

```js
colors: {
  background: '#0D0D0D',
  foreground: '#F5F5F3',
  surface: '#1A1A1A',
  muted: '#888888',
  accent: '#FF3D3D',
  accentSecondary: '#FF8C00',
  neutral: '#2A2A2A',
  card: '#1A1A1A',
},
```

Also add a `destructive` color token below `card`:
```js
destructive: '#EF4444',
```

Keep fontFamily, borderRadius, boxShadow unchanged.

**src/index.css** -- Make these changes:

1. Update `:root` block color values:
```css
:root {
  color: #F5F5F3;
  background-color: #0D0D0D;
}
```

2. Update body background gradient (replace green/blue with red/orange per UI-SPEC):
```css
body {
  @apply bg-background text-foreground font-sans antialiased;
  margin: 0;
  min-height: 100vh;
  background: radial-gradient(circle at 20% 20%, rgba(255,61,61,0.04), transparent 35%),
    radial-gradient(circle at 80% 0%, rgba(255,140,0,0.05), transparent 30%),
    #0D0D0D;
}
```

3. Add new keyframes BEFORE the `prefers-reduced-motion` media query block:

```css
@keyframes timer-card-enter {
  0% { transform: scale(0.9) translateY(8px); opacity: 0; }
  100% { transform: scale(1) translateY(0); opacity: 1; }
}

@keyframes timer-card-exit {
  0% { transform: scale(1); opacity: 1; }
  100% { transform: scale(0.95); opacity: 0; }
}
```

4. Keep existing `celebration-pop`, `float-spark`, scrollbar, and `prefers-reduced-motion` rules unchanged. The `prefers-reduced-motion` block already covers all animations/transitions.
  </action>
  <verify>
    <automated>cd /Users/wesley/Documents/Personal/projects/workout/dani && grep -q "'#0D0D0D'" tailwind.config.js && grep -q "'#FF3D3D'" tailwind.config.js && grep -q "'#FF8C00'" tailwind.config.js && grep -q "destructive" tailwind.config.js && grep -q "rgba(255,61,61" src/index.css && grep -q "timer-card-enter" src/index.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contains `background: '#0D0D0D'`
    - tailwind.config.js contains `accent: '#FF3D3D'`
    - tailwind.config.js contains `accentSecondary: '#FF8C00'`
    - tailwind.config.js contains `destructive: '#EF4444'`
    - tailwind.config.js contains `muted: '#888888'`
    - tailwind.config.js contains `surface: '#1A1A1A'`
    - src/index.css contains `rgba(255,61,61,0.04)` in body gradient
    - src/index.css contains `rgba(255,140,0,0.05)` in body gradient
    - src/index.css contains `@keyframes timer-card-enter`
    - src/index.css contains `@keyframes timer-card-exit`
    - src/index.css `prefers-reduced-motion` block remains at end of file
  </acceptance_criteria>
  <done>Tailwind color tokens updated to new palette, CSS globals reflect new brand, timer animation keyframes ready for Plan 03</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Layout header and 5-tab navigation + add templates placeholder route</name>
  <files>src/components/Layout.tsx, src/App.tsx</files>
  <read_first>src/components/Layout.tsx, src/App.tsx, src/routes/Dashboard.tsx</read_first>
  <action>
**src/components/Layout.tsx** -- Full redesign of header and navigation per UI-SPEC and D-05, D-06, D-07:

1. **Update imports** -- Add `BarChart2`, `LayoutTemplate` from lucide-react. Remove `Dumbbell` import. Remove `Badge`, `Button` imports (no longer used in header). Remove `getCurrentWeekNumber` import. Remove `useWorkoutStore` import. Remove `useActiveProgram` import. Keep `useActiveUserProfile`, `ProfileSwitcher`, `cn`, `Link`, `useLocation`.

2. **Replace navItems array** with 5-tab version (per UI-SPEC Navigation Architecture):
```tsx
const navItems = [
  { to: '/', label: 'Inicio', icon: Home },
  { to: '/week', label: 'Treino', icon: CalendarDays },
  { to: '/progress', label: 'Historico', icon: BarChart2 },
  { to: '/templates', label: 'Templates', icon: LayoutTemplate },
  { to: '/settings', label: 'Config', icon: SettingsIcon },
];
```

3. **Simplify header** per D-06 (UI-SPEC Header Architecture). Remove all program/week info, schedule label, desktop buttons. New header structure:
```tsx
<header className={cn(
  'sticky top-0 z-20 border-b border-neutral/30 bg-background/80 backdrop-blur-md',
  isSession && 'py-2'
)} aria-label='Cabecalho principal'>
  <div className={cn(
    'mx-auto flex max-w-5xl items-center justify-between px-4',
    isSession ? 'py-2' : 'py-3'
  )}>
    <div className='flex items-center gap-3'>
      {isSession ? (
        <div className='leading-tight'>
          <div className='text-xs uppercase tracking-[0.2em] text-muted'>
            Sessao em andamento
          </div>
        </div>
      ) : (
        <h1 className='text-[28px] font-semibold leading-none text-foreground'>
          Dani
        </h1>
      )}
    </div>
    <ProfileSwitcher />
  </div>
</header>
```

4. **Update nav pill** for 5-tab mobile-optimized layout (per UI-SPEC Nav Pill Visual). Replace the active state from `bg-accent text-background` to a foreground icon + accent dot indicator:
```tsx
{!isSession && (
  <nav
    className='fixed bottom-4 left-0 right-0 mx-auto flex justify-center px-4 md:hidden'
    aria-label='Navegacao inferior'
  >
    <div className='flex w-[calc(100%-32px)] max-w-[360px] justify-between rounded-full bg-surface/95 shadow-soft border border-neutral/50 px-2 py-2 backdrop-blur-md'>
      {navItems.map((item) => {
        const active =
          item.to === '/'
            ? location.pathname === '/'
            : location.pathname.startsWith(item.to);
        const Icon = item.icon;
        return (
          <Link
            key={item.to}
            to={item.to}
            className={cn(
              'relative flex flex-1 min-h-[44px] flex-col items-center justify-center gap-1 rounded-full px-1 py-1.5 text-[11px] transition',
              active
                ? 'text-foreground'
                : 'text-muted hover:text-foreground'
            )}
          >
            <Icon className='h-5 w-5' />
            <span>{item.label}</span>
            {active && (
              <span className='absolute bottom-0.5 h-[3px] w-[3px] rounded-full bg-accent' />
            )}
          </Link>
        );
      })}
    </div>
  </nav>
)}
```

5. Remove all variables that are no longer referenced: `navigate`, `program`, `settings`, `weekNumber`, `weekInfo`, `scheduleLabel`. Keep `location`, `profile`, `isSession`.

**src/App.tsx** -- Add templates placeholder route:

1. Add lazy import for a simple placeholder component. Create the route inline since it is trivial:
```tsx
<Route path="/templates" element={
  <div className="grid min-h-[40vh] place-items-center">
    <div className="text-center space-y-2">
      <div className="text-lg font-semibold text-foreground">Templates</div>
      <div className="text-sm text-muted">Em breve</div>
    </div>
  </div>
} />
```

2. Place this route after the `/settings` route and before the catch-all `*` route.
  </action>
  <verify>
    <automated>cd /Users/wesley/Documents/Personal/projects/workout/dani && npx tsc --noEmit 2>&1 | head -20 && grep -q "BarChart2" src/components/Layout.tsx && grep -q "LayoutTemplate" src/components/Layout.tsx && grep -q "Templates" src/components/Layout.tsx && grep -q "/templates" src/App.tsx && grep -q "Em breve" src/App.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with 0 errors
    - Layout.tsx contains `BarChart2` import from lucide-react
    - Layout.tsx contains `LayoutTemplate` import from lucide-react
    - Layout.tsx navItems array has 5 entries including `/templates`
    - Layout.tsx nav tab label says `Treino` (not `Semana`)
    - Layout.tsx header contains `text-[28px]` for "Dani" app name
    - Layout.tsx nav items have `min-h-[44px]` for touch targets
    - Layout.tsx active tab has accent dot indicator (`bg-accent`)
    - Layout.tsx does NOT import `useWorkoutStore` or `useActiveProgram` or `getCurrentWeekNumber`
    - App.tsx contains `path="/templates"` route
    - App.tsx templates route contains "Em breve" text
  </acceptance_criteria>
  <done>5-tab navigation with future-proof structure, simplified header with "Dani" branding, 44px+ touch targets, Templates placeholder route active</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token values | Pure presentational -- no user input crosses boundary |
| Nav route paths | Client-side routing -- no server trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Navigation routes | accept | Client-side SPA routes; no auth boundary to spoof |
| T-03-02 | Information Disclosure | Color contrast | accept | Dark-on-dark mitigated by WCAG AA contrast verification in UI-SPEC (accent on surface meets 4.5:1) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run test:run` passes all existing tests
- `npm run build` succeeds
- tailwind.config.js has all 9 color tokens (background, foreground, surface, muted, accent, accentSecondary, neutral, card, destructive)
- Layout.tsx renders 5 nav tabs
- `/templates` route renders placeholder text
</verification>

<success_criteria>
- App displays new color palette (#0D0D0D background, #FF3D3D accent, #FF8C00 secondary)
- Navigation has 5 tabs with "Treino" replacing "Semana" and new "Templates" placeholder
- Header shows "Dani" app name with ProfileSwitcher on right
- All nav touch targets are 44px+ minimum height
- TypeScript compiles clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-redesign-rest-timer/03-01-SUMMARY.md`
</output>
