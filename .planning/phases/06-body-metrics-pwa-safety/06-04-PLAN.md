---
phase: 06-body-metrics-pwa-safety
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/routes/BodyMetrics.tsx
  - src/components/Layout.tsx
autonomous: false
requirements: [BODY-01, BODY-02, BODY-03, UI-04]

must_haves:
  truths:
    - "All body metrics features work end-to-end in the browser"
    - "Photo gallery is wired into the BodyMetrics route"
    - "iOS install banner renders in Layout above nav"
    - "Full test suite passes"
  artifacts:
    - path: "src/routes/BodyMetrics.tsx"
      provides: "Complete screen with charts, entries, and photo gallery"
      contains: "PhotoGallery"
    - path: "src/components/Layout.tsx"
      provides: "iOS banner slot above nav"
      contains: "iOSInstallBanner"
  key_links:
    - from: "src/routes/BodyMetrics.tsx"
      to: "src/components/PhotoGallery.tsx"
      via: "component import"
      pattern: "PhotoGallery"
    - from: "src/components/Layout.tsx"
      to: "src/components/iOSInstallBanner.tsx"
      via: "component import"
      pattern: "iOSInstallBanner"
---

<objective>
Wire PhotoGallery into the BodyMetrics route, place iOSInstallBanner in Layout, and run a full verification checkpoint with the user.

Purpose: Integrate Plan 02 and Plan 03 outputs into a complete feature, then verify everything works visually and functionally.
Output: 2 modified files + user-verified complete feature
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-body-metrics-pwa-safety/06-UI-SPEC.md
@.planning/phases/06-body-metrics-pwa-safety/06-02-SUMMARY.md
@.planning/phases/06-body-metrics-pwa-safety/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PhotoGallery into BodyMetrics route + iOSInstallBanner into Layout</name>
  <files>src/routes/BodyMetrics.tsx, src/components/Layout.tsx</files>
  <read_first>src/routes/BodyMetrics.tsx, src/components/Layout.tsx, src/components/PhotoGallery.tsx, src/components/iOSInstallBanner.tsx</read_first>
  <action>
    **BodyMetrics.tsx:**
    1. Import `PhotoGallery` from `@/components/PhotoGallery`.
    2. Get photos from store: `const photos = useBodyMetricsStore(s => s.photos)`.
    3. Get activeUserId from workoutStore: `const activeUserId = useWorkoutStore(s => s.activeUserId)`.
    4. Add PhotoGallery section after the entry history list, before the BodyMetricSheet:
    ```tsx
    {/* Photo gallery */}
    <div>
      <div className="text-xs uppercase tracking-[0.2em] text-muted mb-2">Fotos de Progresso</div>
      <PhotoGallery photos={photos} userId={activeUserId} />
    </div>
    ```

    **Layout.tsx:**
    1. Import `iOSInstallBanner` from `@/components/iOSInstallBanner`: `import { iOSInstallBanner as IOSInstallBanner } from './iOSInstallBanner'`
    2. Render `<IOSInstallBanner />` immediately above the `<nav>` element (inside the `{!isSession && (...)}` block, before the nav). This places the banner above the bottom navigation.
    ```tsx
    {!isSession && (
      <>
        <IOSInstallBanner />
        <nav ...>
    ```
    Close the fragment after `</nav>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/BodyMetrics.tsx contains `import` with `PhotoGallery`
    - src/routes/BodyMetrics.tsx contains `<PhotoGallery`
    - src/routes/BodyMetrics.tsx contains `Fotos de Progresso`
    - src/components/Layout.tsx contains `iOSInstallBanner` or `IOSInstallBanner`
    - src/components/Layout.tsx renders banner component above `<nav`
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>PhotoGallery rendered in BodyMetrics screen. iOSInstallBanner rendered in Layout above nav. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full phase verification checkpoint</name>
  <files>n/a</files>
  <action>
    Present the complete body metrics feature for user verification. What was built:
    - Weight logging with quick-log form and trend chart
    - Measurement logging with multi-line chart and legend toggling
    - Progress photos with compression, gallery grid, and full-size viewer
    - iOS install banner with detection and dismissal
    - Export/import extended with progressPhotos

    Verification steps for the user:
    1. Run `npm run dev` and open http://localhost:5173
    2. Tap "Corpo" tab in bottom nav — verify 6 tabs fit without overlap, Scale icon visible
    3. Tap "Registrar" — verify quick-log sheet opens with Peso field autofocused
    4. Enter a weight (e.g., 72.5) and tap "Salvar" — verify toast appears and entry shows in list
    5. Tap "Registrar" again, expand "Medidas (opcional)", enter waist=80, save — verify measurements appear as pills
    6. Verify bodyweight trend chart shows data points
    7. Verify measurements chart appears with waist line (orange)
    8. Tap chart period tabs (1M/3M/6M/1A/Tudo) — verify chart updates
    9. Tap legend item on measurements chart — verify line toggles
    10. Tap entry card — verify edit sheet opens pre-populated
    11. Tap "Excluir" on entry — verify confirmation dialog, then removal
    12. In Photos section, tap "+" to add a photo
    13. Verify photo appears in grid with date badge
    14. Tap photo — verify full-size viewer with delete option
    15. Verify storage indicator shows "X.X MB usados"
    16. (Optional) iOS Safari: verify install banner after ~3 seconds, "Dispensar" hides it
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>User confirms all body metrics features work correctly in browser. All tests pass. Phase 6 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new boundaries) | Integration of existing mitigated components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | Photo dataUrl in DOM | accept | Photos are user's own data rendered via <img src>. No cross-origin risk. Local-only storage. |
</threat_model>

<verification>
```bash
npx tsc --noEmit
npx vitest run --reporter=verbose
```
Visual verification by user at checkpoint.
</verification>

<success_criteria>
- All phase 6 features visible and functional in browser
- 6 nav tabs render without overflow
- Weight + measurement logging works end-to-end
- Charts display data correctly with period filtering
- Photo gallery works with compression
- iOS banner logic correct (testable on iOS Safari)
- Full test suite green
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/06-body-metrics-pwa-safety/06-04-SUMMARY.md`
</output>
