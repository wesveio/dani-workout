---
phase: 04-exercise-history-logging-ux
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: []
autonomous: false
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, HIST-01, HIST-02, HIST-03]

must_haves:
  truths:
    - "Full test suite passes"
    - "User visually confirms set logging flow works end-to-end"
    - "User visually confirms exercise history chart renders correctly"
  artifacts: []
  key_links: []
---

<objective>
Run full test suite and present the app for human verification of the complete Phase 4 feature set: inline set logging with ghost values, PR badges, auto-advance, and exercise history with metric tabs.

Purpose: Ensure all automated tests pass and the user validates the visual/interactive experience before phase sign-off.
Output: Verified phase ready for `/gsd-verify-work`.
</objective>

<execution_context>
@.planning/phases/04-exercise-history-logging-ux/04-RESEARCH.md
@.planning/phases/04-exercise-history-logging-ux/04-UI-SPEC.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-exercise-history-logging-ux/04-02-SUMMARY.md
@.planning/phases/04-exercise-history-logging-ux/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any failures</name>
  <files></files>
  <read_first>
    - src/routes/SessionDetail.test.tsx
    - src/components/SetRow.test.tsx
    - src/components/PrBadge.test.tsx
    - src/components/ExerciseProgressChart.test.tsx
    - src/routes/ExerciseHistory.test.tsx
    - src/lib/epley.test.ts
  </read_first>
  <action>
    Run the full Vitest suite: `npx vitest run`

    If any test fails:
    1. Read the failing test and the source file it tests
    2. Determine if the failure is in the test (wrong assertion) or the source (bug)
    3. Fix the source code if it's a bug; fix the test only if the assertion was wrong
    4. Re-run until all tests pass

    Also run TypeScript check: `npx tsc --noEmit` to catch type errors across the project.
  </action>
  <verify>
    <automated>cd /Users/wesley/Documents/Personal/projects/workout/dani && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - `npx tsc --noEmit` exits with code 0
    - No test failures in output
  </acceptance_criteria>
  <done>Full test suite green. No TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 4 features</name>
  <files></files>
  <action>
    Present the app for human verification. Start dev server with `npm run dev`.
  </action>
  <verify>User approves all Phase 4 features visually and functionally.</verify>
  <done>User types "approved" after verifying all features.</done>
  <what-built>
    Phase 4 Exercise History + Logging UX features:
    1. **Inline set logging** — SetRow component with weight/reps inputs and stepper buttons (LOG-01)
    2. **Ghost values** — Previous session weight/reps shown as "Ant: {value}" below each input (LOG-02)
    3. **Auto-advance** — Focus moves to next incomplete set after completing one (LOG-03)
    4. **PR badge** — Trophy icon + "PR!" badge appears when set weight exceeds all-time best (LOG-04)
    5. **Exercise history** — Metric tab bar (Volume / Carga / 1RM Est.) with dark-theme chart (HIST-01, HIST-02, HIST-03)
    6. **PR summary row** — Best weight, best volume, and best 1RM shown above history list
  </what-built>
  <how-to-verify>
    Start dev server: `npm run dev`

    **Set Logging Flow (SessionDetail):**
    1. Open an active workout session
    2. Verify each set row shows weight and reps inputs with stepper buttons
    3. If this exercise has been logged before: verify "Ant: {value}" ghost text appears below weight and reps inputs in muted gray
    4. Enter a weight HIGHER than your previous best for this exercise
    5. Tap "Marcar feito" — verify:
       - A "PR!" badge with Trophy icon appears next to the completed set
       - The badge has a pop animation on mount
       - Focus automatically moves to the next incomplete set's weight input
    6. Complete another set with weight EQUAL to your best — verify NO PR badge appears
    7. Un-complete a set (toggle off) — verify focus does NOT jump

    **Exercise History (ExerciseHistory):**
    1. Navigate to Historico tab
    2. Tap any exercise to open its history
    3. Verify three metric tabs appear: "Volume", "Carga", "1RM Est."
    4. Verify "Volume" tab is active by default showing an orange (#FF8C00) area chart
    5. Switch to "Carga" — verify red (#FF3D3D) area chart
    6. Switch to "1RM Est." — verify red (#FF3D3D) area chart
    7. Verify tooltip has dark background (#1A1A1A) when hovering chart
    8. Verify PR summary row above chart shows "Melhor carga:", "Maior volume:", "1RM Est. max:" with correct values
    9. Scroll down to verify history log list renders with session entries

    **Visual checks:**
    - All colors match dark theme (no green #18D02E remnants in chart)
    - Ghost values are 12px muted gray, not distracting
    - PR badge is accent red with Trophy icon
    - Touch targets are comfortable for mobile use
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — verification only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Verification plan, no new code |
</threat_model>

<verification>
Full suite: `npx vitest run` — all pass
TypeScript: `npx tsc --noEmit` — no errors
Human: visual/functional verification of all Phase 4 features
</verification>

<success_criteria>
- All automated tests pass
- No TypeScript errors
- User approves visual and functional behavior of all Phase 4 features
</success_criteria>

<output>
After completion, create `.planning/phases/04-exercise-history-logging-ux/04-04-SUMMARY.md`
</output>
